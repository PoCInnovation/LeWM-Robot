# Core ML
torch>=2.4              # transformers récent demande >= 2.4
torchvision>=0.19       # requis par AutoImageProcessor
transformers>=4.40
numpy>=1.24
einops>=0.7

# DINOv3
# Loaded via HuggingFace transformers (no special install)

# Datasets
datasets>=2.14
huggingface-hub>=0.20

# LeRobot for SO-101 dataset loading
# IMPORTANT : la version PyPI "lerobot 0.1.0" est OBSOLÈTE (ancien repo Cadène).
# Installer la vraie version HuggingFace depuis GitHub :
#   pip install git+https://github.com/huggingface/lerobot.git
# Ou décommenter la ligne suivante (peut casser selon les versions de torch) :
# lerobot @ git+https://github.com/huggingface/lerobot.git

# Vidéo decoding (lerobot 0.5.2 demande pyav < 17)
av>=15,<17

# Diffusers récent (pour éviter le bug cached_download avec huggingface_hub récent)
diffusers>=0.30

# Image handling
Pillow>=10.0
opencv-python-headless>=4.8

# Plotting / analysis
matplotlib>=3.7
pandas>=2.0
scikit-learn>=1.3

# Progress bars
tqdm>=4.65

# Utility
pyyaml>=6.0

# Reproductibilité / config
omegaconf>=2.3        # optionnel : configs avancées
